<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record model="ir.ui.view" id="membership_members_tree">
        <field name="name">res.partner.tree.member_of</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="membership.membership_members_tree" />
        <field name="arch" type="xml">

            <field name="user_id" position="after">
                <field name="customer_of" widget="many2many_tags" optional="show" />
            </field>
        </field>
    </record>
</odoo>
