<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="membership_billing_menu.list">
      <field name="name">membership_billing_menu list</field>
      <field name="model">membership_billing_menu.membership_billing_menu</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="membership_billing_menu.action_window">
      <field name="name">membership_billing_menu window</field>
      <field name="res_model">membership_billing_menu.membership_billing_menu</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="membership_billing_menu.action_server">
      <field name="name">membership_billing_menu server</field>
      <field name="model_id" ref="model_membership_billing_menu_membership_billing_menu"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="membership_billing_menu" id="membership_billing_menu.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="membership_billing_menu.menu_1" parent="membership_billing_menu.menu_root"/>
    <menuitem name="Menu 2" id="membership_billing_menu.menu_2" parent="membership_billing_menu.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="membership_billing_menu.menu_1_list" parent="membership_billing_menu.menu_1"
              action="membership_billing_menu.action_window"/>
    <menuitem name="Server to list" id="membership_billing_menu" parent="membership_billing_menu.menu_2"
              action="membership_billing_menu.action_server"/>
    -->
  </data>
</odoo>